<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group5.interviewready">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InterviewReady">
        <activity android:name=".PreviousRecordsActivity"></activity>
        <activity android:name=".AptiStudyActivity" />
        <activity android:name=".DsStudyActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".ChooseSubjectActivity" />
        <activity android:name=".OptionActivity" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>